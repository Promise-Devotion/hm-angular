import { FormatterSexPipe } from './formatter-sex.pipe';

describe('FormatterSexPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatterSexPipe();
    expect(pipe).toBeTruthy();
  });
});
