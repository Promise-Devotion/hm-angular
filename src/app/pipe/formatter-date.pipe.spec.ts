import { FormatterDatePipe } from './formatter-date.pipe';

describe('FormatterDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatterDatePipe();
    expect(pipe).toBeTruthy();
  });
});
